import { Slide } from 'react-slideshow-image';

interface Props {
  images: string[];
}

export const ProductSlideshow: React.FC<Props> = ({images}) => {

  return (
    <Slide>
      
    </Slide>
  );
};
