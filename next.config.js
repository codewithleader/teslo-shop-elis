/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: false, // TODO: falta comentar...la comenté para que sea mas rapido en desarrollo. SWC es como Babel
}

module.exports = nextConfig
